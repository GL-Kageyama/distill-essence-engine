# バックアップ（旧設定の退避場所）

「明確に古い設定」を、消さずにここへ退避する。カードとして走査されないよう `prompt.md` という名前は使わない。

- `niji-character-sheet--v1-translucent.md` — ニジ設定画の旧版（v1）。「輪郭がうっすらぼやけ、体がほのかに透ける」「faintly translucent」を常時特性として書いていた矛盾（開示台帳：不透明 10–38 → 薄い 39–40 → 白い光 52–56 の段階と矛盾）を修正する前の版。git HEAD から回収（2026-08-30）。
- `wan-spec-paste/` — 各セグメントの `wan-spec.md`（§7–20）と `paste.md`（貼り付け用）計114ファイル。生成には使わない（`wan-full-spec.md` のみ使用）ため待避（2026-08-30）。元の `gozen-niji-video-XX/seg-YY/` 構造を保って退避している。
