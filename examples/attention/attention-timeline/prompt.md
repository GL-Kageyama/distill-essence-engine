# Attention 論文 → タイムライン

- 入力: [input.md](input.md)（arXiv 1706.03762：Transformer の系譜・機構・比較・数値）
- format: タイムライン（説明・全弧×畳み込み・横長バンド）—— レジストリ追加カード（0.1.20・検証対象）
- style: バウハウス系ベクター（検証済み）
- 目的: 理解

## 内容（Content）
語る一点＝**2017 の転回点**。「ひとつずつ処理する」時代（逐次の鎖）が、2017 で「一斉に重み付きで結ぶ」網へ切り替わる。鎖が一本の太い節（Transformer）で折れ、そこから全弧へ届く接続線が放たれる——転回点が主役、その前後は最小限。

## フォーマット（Format）
横長バンドの一本の時間軸（1980s–2018）。時代を余白で区切って階層とし、転回点（2017）を大きく濃い節点に。節点は最小限の年号ラベルのみ。時間の幅が一目で読めること。

## 様式（Style）
バウハウス系フラットベクター：機能的なグリッド、平坦な色面、一斉接続の線だけにアクセント色、細いサンセリフ、装飾なし。

## 合成プロンプト（Merged）
A wide horizontal Bauhaus-style flat vector timeline of sequence-transduction approaches, one continuous time axis from the 1980s to 2018, era spans separated by calm whitespace, minimal year labels. The recurrent/convolutional era (1980s–2017) drawn as a thin sequential chain of small nodes, one after another; then a single larger darker turning-point node at 2017, the Transformer, where the chain breaks into a fan of weighted links reaching across the whole span to the 2018 era (BERT, GPT) built from those global links — the turning point is the protagonist, its before and after minimal. Flat color planes, functional grid, one accent color on the all-at-once links, thin sans-serif, generous whitespace. Not photorealistic, no 3D render, no decorative illustration, no clutter.
